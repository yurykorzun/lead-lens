# Server
PORT=3001
NODE_ENV=development

# Salesforce API
SF_LOGIN_URL=https://leonbelov.my.salesforce.com
SF_CONSUMER_KEY=<paste Consumer Key from Connected App>
SF_CONSUMER_SECRET=<paste Consumer Secret from Connected App>
SF_API_VERSION=v62.0

# Neon Postgres
DATABASE_URL=<paste connection string from Neon dashboard>

# JWT (for app's own auth tokens)
APP_JWT_SECRET=<generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))">
APP_JWT_EXPIRES_IN=8h

# CORS
FRONTEND_URL=http://localhost:3000
